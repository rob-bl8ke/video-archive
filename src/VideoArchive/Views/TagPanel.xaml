<UserControl
    x:Class="VideoArchive.Views.TagPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Tags"
                   Style="{ThemeResource BodyStrongTextBlockStyle}"
                   Margin="0,0,0,8" />

        <!-- "No video" hint -->
        <TextBlock x:Name="NoVideoHint"
                   Grid.Row="1"
                   Text="Select a video to manage its tags."
                   Style="{ThemeResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" />

        <!-- Selection subtitle (video title or "N videos selected") -->
        <TextBlock x:Name="SelectionSubtitle"
                   Grid.Row="1"
                   Visibility="Collapsed"
                   Style="{ThemeResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextTrimming="CharacterEllipsis" />

        <!-- Tag checkboxes -->
        <ScrollViewer x:Name="TagScroller"
                      Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="Collapsed">
            <StackPanel x:Name="TagList" Spacing="4" />
        </ScrollViewer>
    </Grid>
</UserControl>
